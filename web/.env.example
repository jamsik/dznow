# Пока строки нет — приложение работает без сервера:
# проекты лежат в браузере, картинка собирается на устройстве.
#
# Как только API поднят (api\run.ps1), скопируйте этот файл в web\.env —
# фронт начнёт сохранять проекты на сервере и просить у него настоящий PNG.
VITE_API_BASE=/api
