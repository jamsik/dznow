/**
 * Планировка в макете — только то, что загрузил пользователь.
 * В режиме «чертёж» картинка уже очищена и перекрашена (lib/planImage.js),
 * в режиме «как есть» показывается оригинал.
 *
 * Своих планировок приложение не придумывает: показывать выдуманную геометрию
 * реальной квартиры нельзя. Нет файла — блока в макете нет.
 */
export default function FloorPlan({ image }) {
  if (!image) return null;
  return <img src={image} alt="Планировка" />;
}
