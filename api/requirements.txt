# Версии намеренно не прибиты гвоздями: playwright 1.49 требовал greenlet==3.1.1,
# у которого нет колеса под Python 3.14 — pip пытался компилировать его через MSVC.
# Диапазоны дают pip выбрать сборки под вашу версию Python.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
playwright>=1.50
python-dotenv>=1.0
